# pcoc-agent calibration audit log

This file is the **committed** history of skill calibrations and
promotions. It contains hashes and decisions, not payloads.

For session-level forensic detail (blocked paths, override reasons,
specific session IDs), see `audit_log.local.md` — which is gitignored.

Format: each entry is a level-2 heading with ISO timestamp + action.

---

(no entries yet — this file grows as you run calibrations and promote
skills.)
